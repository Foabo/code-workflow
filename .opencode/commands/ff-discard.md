---
description: Abandon a task after user-confirmed worktree handling, then remove the task record.
---

Use this OpenCode slash command for the `ff-discard` Flowflow workflow action in this repository.

Load and follow the generated Flowflow skill guidance from:
@.agents/skills/ff-discard/SKILL.md

User request from slash command arguments:
`$ARGUMENTS`

If `.agents/skills/ff-discard/SKILL.md` cannot be loaded, stop and report the missing generated skill. Do not continue from memory or from this shim alone.
