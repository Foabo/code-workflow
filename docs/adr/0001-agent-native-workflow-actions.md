# Agent-native workflow actions with deterministic kernel helpers

The workflow kernel exposes daily work through harness-native `ff-*` workflow actions: `ff-work`, `ff-clarify`, `ff-plan`, `ff-run`, `ff-check`, `ff-finish`, `ff-resume`, `ff-discard`, and `ff-understand`. Support commands such as `ff-init`, `ff-doctor`, `ff-update`, and validation commands cover repository setup, diagnostics, schema validation, adapter generation, and automation. All surfaces delegate deterministic state changes to kernel helpers, so Codex, Claude Code, OpenCode, and CLI entries write the same `.ff` task facts.
