# Task spec evolves within a task

Flowflow will keep a task-local `spec.md` as the current contract for that task. Clarification writes or updates the task spec, and implementation, verification, or review may require updating the task spec when the task's goal, scope, constraints, decisions, or acceptance criteria change. Project baseline files are updated separately during finish after the user confirms which stable facts should be promoted.
