# Understand drafts before baseline update

CW understand will analyze an existing repository into draft project-baseline files first, rather than writing directly to `.cw/project/`. The user reviews the generated overview, architecture, rules, and commands drafts; accepted content is then semantically merged into the project baseline by the agent with helper validation and trace recording.
