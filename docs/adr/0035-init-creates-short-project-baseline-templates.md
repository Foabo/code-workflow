# Init creates short project baseline templates

CW init will create short template files for `.cw/project/overview.md`, `architecture.md`, `rules.md`, and `commands.md`. These files start as sparse project-baseline placeholders, then are filled by `cw-understand` for existing repositories or by finish-time baseline delta promotion as real tasks produce stable reusable facts.
