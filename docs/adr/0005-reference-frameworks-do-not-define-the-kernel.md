# Reference frameworks do not define the kernel

Flowflow will use its own workflow actions, state machine, repository truth, and task artifacts as the core product shape. Projects such as Trellis, OpenSpec, Gentle-AI, and Oh My OpenCode Slim may inform design choices or adapters, but they will not become core runtime dependencies or define the canonical `.ff` format.
