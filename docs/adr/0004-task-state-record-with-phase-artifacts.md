# Task state record with task artifacts

The repository truth layer will keep task state in a machine-readable task state record while keeping task-local working documents as human-readable artifacts. This gives the kernel stable state for validation, recovery, diagnostics, and adapter generation, while preserving Markdown artifacts that coding agents and humans can read and edit naturally.
