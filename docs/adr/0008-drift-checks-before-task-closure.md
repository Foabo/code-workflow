# Drift checks before task closure

CW will check for drift between task facts and implementation changes during verification, review, and closure. If implementation work changes the intended behavior, constraints, acceptance criteria, or execution path, the task must update its spec, plan, or context, or repair the implementation before it can be closed.
