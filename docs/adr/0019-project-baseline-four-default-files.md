# Project baseline uses four default files

CW will use a concise project baseline under `.cw/project/` with four default files: `overview.md`, `architecture.md`, `rules.md`, and `commands.md`. Optional files such as `glossary.md` may be created when a project has confirmed terminology needs, but they are not part of the default baseline because many projects do not need a separate glossary.
