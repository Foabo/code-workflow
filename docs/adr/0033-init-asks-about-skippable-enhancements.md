# Init asks about skippable enhancements

Flowflow init will ask about optional enhancements such as code intelligence and external memory or context tools after the required harness-entry selection. Users can skip every enhancement, and the core workflow must remain fully usable with only `.ff` task facts, project baseline files, agent commands, and kernel helpers. Enhancement choices configure recommendations or detection behavior; they do not become repository truth or task workflow prerequisites.
