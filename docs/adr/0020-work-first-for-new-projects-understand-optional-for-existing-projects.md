# Work first for new projects, understand optional for existing projects

Flowflow will not require new projects to build a full project baseline during initialization. New projects start with `init -> work`, and stable project understanding is promoted into the project baseline as real tasks clarify goals, architecture, rules, and commands. Existing projects may run `understand` to draft or refresh the project baseline from current repository facts, but `understand` remains optional and does not block task workflow.
