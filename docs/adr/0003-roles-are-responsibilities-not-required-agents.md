# Workflow roles are responsibilities, not required separate agents

The kernel will define workflow roles as responsibility profiles, but an invocation surface may combine multiple roles when that gives a better user experience. This preserves clear ownership of clarification, specification, planning, context building, implementation, verification, review, finish, and optional resume-note writing without requiring every task to involve separate subagents for every responsibility.
